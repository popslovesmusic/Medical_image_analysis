# Default configuration file for the trainer.
# This file serves as a template and a fallback.
# As specified in `config/spec.md`.

[engine]
base_seed = 42
batch_size = 32
epochs = 10
learning_rate = 0.001

[model]
model_type = "DeterministicCNN"
layers = 5
activation = "ReLU"