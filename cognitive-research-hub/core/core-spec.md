Specification Sheet: Core Crate (core/)The core/ crate is the primary library containing the ZAG-constrained logic for execution and archiving.1. core/Cargo.toml SpecificationThis file defines the crate's identity, the required Rust edition, and locks in the external and workspace dependencies necessary for the high-fidelity, deterministic pipeline.SectionKeys/DependenciesRationale & Constraint[package]name = "chromatic-core", edition = "2021"Mandatory: Establishes the library identity and the target Rust edition.[dependencies]ndarray = "0.15" (Array ops) $\text{ray on}$ (Parallelism) $\text{serde} / \text{bincode}$ (Checkpointing) $\text{toml}$ (Config loading)Scientific ZAG: Provides the fast array structures ($\text{ndarray}$) and safe parallelism ($\text{Rayon}$) needed for $\text{UMS}$ encoding [cite: Final Structural Specification]. $\text{Bincode}$ is mandatory for deterministic checkpointing [cite: Final Structural Specification].[dev-dependencies]approx, criterionNeeded for high-precision testing ($\mathbf{\Delta E_{94}}$) and performance benchmarking.Workspace Dep.$\text{tiny-agent-trainer} = \{ \text{path} = \text{"../trainer"} \}$Monorepo Link: Enables the Core to access the Trainer's output paths and utility functions safely within the workspace.GPU/FFT/SIMD$\text{wgpu}$ (GPU), $\text{rustfft}$ (FFT), $\text{half}$ (f16 UMS compression)Required: Links the core logic to the necessary low-level performance libraries (FFT/GPU/f16 compression) [cite: Final Structural Specification].