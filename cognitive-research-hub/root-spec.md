Next Specification: Root Directory and Cargo.tomlThe next step is to define the file contents of the root directory of your monorepo, which governs the entire project's build and dependency management.1. Root Cargo.toml SpecificationThis file defines the Cargo Workspace and establishes the project's Rust Edition and Dependency Resolver.FileContentRationaleCargo.toml[workspace] table listing core and trainer as members [cite: 2.7]. resolver = "2" (or $\mathbf{\text{"3"}}$) [cite: 1.2].Mandatory: Establishes the monorepo structure and enables shared build infrastructure [cite: 1.1].2. Root Directory FilesThese files are essential for project management and auditing, as specified in the AGENTS.md protocol.FilePurposeRationaleAGENTS.mdProject Management ManualContains all $\text{ZAG}$ constraints, architectural rules, and phase overviews [cite: AGENTS.md: Project Management Manual].scripts/Build & Transfer ScriptsHolds the auditable build scripts (generate_all_templates.sh, etc.) [cite: generate_all_templates.sh, generate_all_templates.bat].